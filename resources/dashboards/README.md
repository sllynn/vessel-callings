# dashboards

Lakeview dashboards will live here as `*.lvdash.json` files, registered via
sibling `*.yml` bundle resources. Held back from the initial scaffold because
a Lakeview JSON must be authored against a live warehouse + populated tables,
both of which arrive in later iterations.
